## Electron app to export web page as a Native app
Helps in running applications like Google Keep separate from Chrome Browser

## Setup
```
$ npm install
$ npm run build # will generate app in `./build`
```
